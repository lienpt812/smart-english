
# SmartEnglish Frontend

Vite React interface for SmartEnglish. The app talks to the Backend API on `http://localhost:4000` and Supabase through Vite environment variables.

## Running

```powershell
npm install
npm run dev
```

Open `http://localhost:3000`.
